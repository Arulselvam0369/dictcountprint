def most_frequent(string):
    dict={}
    for i in string:
        keys=dict.keys()
        if i in keys:
            dict[i]+=1
        else:
            dict[i]=1
    return dict
print(most_frequent('mississippi'))
